[project]
name = "openops-ai"
version = "0.1.0"
description = "Enterprise Autonomous Incident Response Platform"
requires-python = ">=3.11"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "pydantic-settings",
    "pymongo",
]